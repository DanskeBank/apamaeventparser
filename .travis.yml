language: python
python:
  - "2.7"
  - "3.5"
install:
  - pip install -r requirements.txt
  - pip install coveralls
before_script:
  - export PYTHONPATH=$(pwd);$PYTHONPATH
script: coverage run --omit tests/* -m unittest discover -v -s tests
after_success:
  coveralls